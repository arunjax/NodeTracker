<div class="page-header">
  <h1>Details of user</h1>
</div>

<div class="actions">
  <%- link_to('Edit', path_to.edit_user(member), {class: 'btn primary'}) %>
  <%- link_to('Delete', path_to.user(member), {class: 'btn danger', method: 'delete', remote: true, jsonp: '(function (u) { location.href = u; })'}) %>
  or
  <%- link_to('Back to index', path_to.users) %>
</div>
